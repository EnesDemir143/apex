---
phase: 7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apex/agents/workflow.py
  - src/apex/agents/checkpoint.py
  - src/apex/agents/compaction.py
autonomous: true
requirements: [AGENT-06, AGENT-07, AGENT-09, LSMI-02]
---

<objective>
Assemble the full LangGraph StateGraph workflow with parallel agent execution, PostgreSQL checkpoint persistence via AsyncPostgresSaver (auto-created tables), context compaction, and agent decision persistence to analysis_runs + agent_decisions tables.
</objective>

<must_haves>
- truths:
  - "StateGraph: parallel (Tech+Fund+Risk) → PM"
  - "PostgreSQL checkpoint via AsyncPostgresSaver.setup() — auto-creates 4 tables, NOT in Alembic"
  - "Context compaction reduces tokens when budget exceeded"
  - "Full workflow traced end-to-end in LangSmith with project=apex"
  - "Each agent decision persisted to agent_decisions table with reasoning + token usage"
  - "Analysis run persisted to analysis_runs table with UUID correlation_id"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create StateGraph workflow assembly</title>
<files>src/apex/agents/workflow.py</files>
<read_first>
- src/apex/agents/state.py
- src/apex/agents/technical.py
- src/apex/agents/fundamental.py
- src/apex/agents/risk.py
- src/apex/agents/portfolio_manager.py
- src/apex/agents/hooks.py
</read_first>
<action>
Create workflow.py: build StateGraph with pre_hook → [technical, fundamental, risk] (parallel) → post_hook → portfolio_manager → END. Export `create_workflow()` function that returns compiled graph. Include create_workflow_with_checkpointer() variant.

**LangSmith Integration:**
- When invoking the workflow, pass `config={"run_name": f"analyze_{ticker}", "metadata": {"ticker": ticker, "project": "apex"}}` so each full analysis run is a single LangSmith trace with all agent sub-runs nested underneath
- The `LANGCHAIN_TRACING_V2=true` env var (from Phase 1 Settings) auto-enables the tracer — no manual setup needed
- Each nested agent node appears as a child span inside the parent workflow trace
</action>
<acceptance_criteria>
- workflow.py contains `StateGraph(AgentState)`
- workflow.py contains `add_node` calls for all agents
- workflow.py contains `def create_workflow`
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create checkpoint saver and compaction</title>
<files>src/apex/agents/checkpoint.py, src/apex/agents/compaction.py</files>
<read_first>
- src/apex/core/config.py
- src/apex/agents/state.py
</read_first>
<action>
Create checkpoint.py: async PostgreSQL checkpoint saver setup using `langgraph.checkpoint.postgres.aio.AsyncPostgresSaver`.
- Use `psycopg_pool.AsyncConnectionPool` with `row_factory=dict_row` and `autocommit=True`
- Call `await checkpointer.setup()` to auto-create 4 tables (checkpoints, checkpoint_blobs, checkpoint_writes, checkpoint_migrations)
- Do NOT create manual checkpoint tables or Alembic migrations for checkpoints
- Optionally support custom schema parameter for isolation

Create compaction.py: compact_agent_context(state, token_budget) that summarizes verbose outputs, sets compaction_applied=True.
</action>
<acceptance_criteria>
- checkpoint.py contains `AsyncPostgresSaver`
- checkpoint.py contains `await checkpointer.setup()` or equivalent
- checkpoint.py does NOT contain CREATE TABLE or Alembic references
- compaction.py contains `def compact_agent_context`
- compaction.py contains `compaction_applied`
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `create_workflow()` returns a compiled graph
2. Workflow accepts AgentState and produces portfolio_decision
3. AsyncPostgresSaver.setup() creates checkpoint tables automatically
4. No checkpoint tables in Alembic migrations
</verification>

<success_criteria>
- Full workflow assembled with parallel execution
- Checkpoint persistence via AsyncPostgresSaver (auto-created tables)
- Context compaction functional
- Agent decisions persisted to agent_decisions table
</success_criteria>
