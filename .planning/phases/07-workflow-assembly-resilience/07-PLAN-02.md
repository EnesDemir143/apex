---
phase: 7
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apex/agents/circuit_breaker.py
  - src/apex/agents/retry.py
  - src/apex/agents/fallback.py
  - src/apex/services/llm_cache.py
autonomous: true
requirements: [RES-01, RES-02, RES-03, RES-04, CACHE-02]
---

<objective>
Implement resilience patterns: circuit breaker with Redis, retry with exponential backoff, rule-based fallback, graceful error propagation, and LLM response caching.
</objective>

<must_haves>
- truths:
  - "Circuit breaker opens after repeated failures"
  - "Rule-based fallback activates when circuit open"
  - "Failed agent doesn't crash workflow"
  - "LLM responses cached in Redis with 24h TTL"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create circuit breaker and retry</title>
<files>src/apex/agents/circuit_breaker.py, src/apex/agents/retry.py</files>
<read_first>
- src/apex/infrastructure_layer/redis_client.py
</read_first>
<action>
Create circuit_breaker.py: CircuitBreaker class with Redis state persistence, failure_threshold=3, recovery_timeout=60s, CLOSED/OPEN/HALF_OPEN states.
Create retry.py: async retry decorator with exponential backoff (base=1s, max=30s, max_retries=3).
</action>
<acceptance_criteria>
- circuit_breaker.py contains `class CircuitBreaker`
- circuit_breaker.py contains `OPEN` and `CLOSED` and `HALF_OPEN`
- retry.py contains exponential backoff logic
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create fallback and LLM cache</title>
<files>src/apex/agents/fallback.py, src/apex/services/llm_cache.py</files>
<read_first>
- src/apex/agents/state.py
- src/apex/services/cache_service.py
</read_first>
<action>
Create fallback.py: rule_based_fallback(state) using RSI rules only, confidence=0.3, source="rule_based".
Create llm_cache.py: LLMCacheService wrapping LLMClient, cache key = hash(ticker+date+agent+prompt_version), 24h TTL.
</action>
<acceptance_criteria>
- fallback.py contains `def rule_based_fallback`
- fallback.py contains `confidence` and `0.3`
- llm_cache.py contains cache key hashing
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Circuit breaker opens after 3 failures
2. Fallback returns valid signal with confidence 0.3
3. LLM cache stores and retrieves
</verification>

<success_criteria>
- Resilience patterns fully implemented
- LLM caching operational
</success_criteria>
