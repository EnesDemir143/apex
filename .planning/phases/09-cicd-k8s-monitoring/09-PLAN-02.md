---
phase: 9
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/base/kustomization.yaml
  - k8s/base/namespace.yaml
  - k8s/base/deployment-api.yaml
  - k8s/base/deployment-frontend.yaml
  - k8s/base/service-api.yaml
  - k8s/base/service-frontend.yaml
  - k8s/base/configmap.yaml
  - k8s/base/secrets.yaml
  - k8s/base/cronjob.yaml
  - k8s/base/ingress.yaml
  - k8s/base/networkpolicy.yaml
  - k8s/overlays/local/kustomization.yaml
  - k8s/overlays/production/kustomization.yaml
autonomous: true
requirements: [K8S-01, K8S-02, K8S-03]
---

<objective>
Create K8s manifests for K3s v1.34 with Kustomize base + overlays for local and production environments.
</objective>

<must_haves>
- truths:
  - "K8s manifests deploy to K3s v1.34"
  - "Kustomize base + overlays (local, production)"
  - "CronJob + Ingress + Network Policies"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create Kustomize base manifests</title>
<files>k8s/base/ (all files)</files>
<read_first>
- Dockerfile
- .env.example
</read_first>
<action>
Create namespace (apex), deployments (api, frontend), services (ClusterIP), configmap from env vars, secrets template, cronjob for scheduled analysis, nginx ingress, network policies restricting inter-pod traffic.
</action>
<acceptance_criteria>
- k8s/base/namespace.yaml contains `name: apex`
- k8s/base/deployment-api.yaml contains container spec with health probes
- k8s/base/kustomization.yaml lists all resources
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create Kustomize overlays</title>
<files>k8s/overlays/local/kustomization.yaml, k8s/overlays/production/kustomization.yaml</files>
<read_first>
- k8s/base/kustomization.yaml
</read_first>
<action>
Create local overlay: single replica, no resource limits.
Create production overlay: 2+ replicas, resource limits, production env vars.
</action>
<acceptance_criteria>
- overlays/production/kustomization.yaml contains `replicas` patch
- `kustomize build k8s/overlays/local` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `kustomize build k8s/overlays/local` generates valid YAML
2. `kubectl apply --dry-run=client -f -` validates output
</verification>

<success_criteria>
- K8s manifests valid and deployable to K3s
- Kustomize overlays for both environments
</success_criteria>
