---
phase: 9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/cd.yml
  - .pre-commit-config.yaml
autonomous: true
requirements: [CICD-01, CICD-02, CICD-03]
---

<objective>
Create GitHub Actions CI/CD pipelines and pre-commit hooks for automated quality checks and Docker image publishing.
</objective>

<must_haves>
- truths:
  - "CI: uv sync --frozen + ruff + mypy + pytest"
  - "CD: Docker multi-arch (ARM64+AMD64) push to ghcr.io"
  - "Pre-commit: uvx ruff, uvx mypy"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create CI and CD workflows</title>
<files>.github/workflows/ci.yml, .github/workflows/cd.yml</files>
<read_first>
- pyproject.toml
- Dockerfile
</read_first>
<action>
Create ci.yml: Python 3.13, setup-uv, uv sync --frozen, ruff check, mypy src/, pytest --cov.
Create cd.yml: Docker buildx, multi-arch (amd64+arm64), push ghcr.io, tag with SHA and latest.
</action>
<acceptance_criteria>
- ci.yml contains `uv sync --frozen`
- ci.yml contains `ruff check`
- ci.yml contains `mypy`
- ci.yml contains `pytest`
- cd.yml contains `linux/amd64,linux/arm64`
- cd.yml contains `ghcr.io`
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create pre-commit config</title>
<files>.pre-commit-config.yaml</files>
<read_first>
- pyproject.toml
</read_first>
<action>
Create .pre-commit-config.yaml with ruff (check + format) and mypy hooks using uvx.
</action>
<acceptance_criteria>
- .pre-commit-config.yaml contains `ruff`
- .pre-commit-config.yaml contains `mypy`
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `act -j ci` or GitHub Actions validates ci.yml syntax
2. Pre-commit config parseable
</verification>

<success_criteria>
- CI pipeline defined and validated
- CD pipeline for multi-arch Docker
- Pre-commit hooks configured
</success_criteria>
