---
phase: 9
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/apex/infrastructure_layer/otel.py
  - docker/observability/tempo-config.yml
  - docker/observability/prometheus.yml
  - docker/observability/otel-collector-config.yml
  - docker-compose.dev.yml
  - scripts/backup_postgres.sh
  - scripts/restore_postgres.sh
autonomous: true
requirements: [MON-01, MON-02, MON-03, OPS-01]
---

<objective>
Add OpenTelemetry auto-instrumentation, Grafana Tempo for traces, Prometheus for metrics, OTel Collector, log↔trace correlation, and PostgreSQL backup scripts.
</objective>

<must_haves>
- truths:
  - "OTel auto-instruments FastAPI, traces in Grafana Tempo"
  - "Logs in Loki correlate with traces via trace_id"
  - "Prometheus metrics visible in Grafana"
  - "PostgreSQL backup script creates and uploads"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Add OTel instrumentation and collector</title>
<files>src/apex/infrastructure_layer/otel.py, docker/observability/otel-collector-config.yml</files>
<read_first>
- src/apex/api/app.py
- src/apex/core/config.py
</read_first>
<action>
Create otel.py: setup_otel() function that configures TracerProvider, OTLP exporter, FastAPIInstrumentor.
Create OTel Collector config receiving OTLP, exporting to Tempo and Prometheus.
</action>
<acceptance_criteria>
- otel.py contains `FastAPIInstrumentor`
- otel.py contains `TracerProvider`
- otel-collector-config.yml contains `otlp` receiver
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Add Tempo, Prometheus, and update compose</title>
<files>docker/observability/tempo-config.yml, docker/observability/prometheus.yml, docker-compose.dev.yml</files>
<read_first>
- docker-compose.dev.yml
</read_first>
<action>
Create Tempo config. Create Prometheus config scraping OTel Collector. Update docker-compose.dev.yml adding tempo, prometheus, otel-collector services. Update Grafana datasources to include Tempo and Prometheus.
</action>
<acceptance_criteria>
- docker-compose.dev.yml contains `tempo` service
- docker-compose.dev.yml contains `prometheus` service
- docker-compose.dev.yml contains `otel-collector` service
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Create PostgreSQL backup scripts</title>
<files>scripts/backup_postgres.sh, scripts/restore_postgres.sh</files>
<read_first>
- docker-compose.dev.yml
</read_first>
<action>
Create backup_postgres.sh: pg_dump with compression, timestamped filename, optional OCI upload.
Create restore_postgres.sh: pg_restore from backup file.
Both scripts chmod +x.
</action>
<acceptance_criteria>
- backup_postgres.sh contains `pg_dump`
- restore_postgres.sh contains `pg_restore`
- Both files are executable
</acceptance_criteria>
</task>

</tasks>

<verification>
1. OTel instrumentation initializes without errors
2. Traces appear in Tempo after API call
3. Backup script creates dump file
</verification>

<success_criteria>
- Full observability stack operational
- Traces, metrics, and logs correlated in Grafana
- PostgreSQL backups functional
</success_criteria>
