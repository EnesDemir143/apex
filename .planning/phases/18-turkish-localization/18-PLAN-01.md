---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: [15, 17]
files_modified:
  - src/apex/services/local_analysis.py
  - src/apex/reports/markdown.py
  - src/apex/tui/app.py
  - src/apex/tui/widgets.py
  - tests/unit/test_localization.py
autonomous: false
branch: feat/turkish-localization
requirements: [TUI-11]
---

<objective>
Add optional Turkish output/report language after the English-first cockpit is stable.
</objective>

<branching>
Before implementation, create/switch to feature branch: `feat/turkish-localization`. Use standard feature branch naming (`feat/<slug>`), not `bet5/` or `codex/`. Keep commits for this phase on that branch.
</branching>

<tasks>
<task id="1">
<type>execute</type>
<title>Add output language setting</title>
<files>src/apex/services/local_analysis.py, src/apex/tui/app.py, src/apex/tui/widgets.py</files>
<action>
Add output language to setup/config with English as default and Turkish as optional. Pass selected language into local analysis state/prompt config.
</action>
<acceptance_criteria>
- English remains default.
- Turkish can be selected explicitly.
- Metadata records selected language.
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Generate Turkish report output when selected</title>
<files>src/apex/reports/markdown.py</files>
<action>
Adjust report rendering and agent prompt language instructions so Turkish mode produces Turkish report prose while preserving tickers, numbers, signals, and structured metadata.
</action>
<acceptance_criteria>
- Turkish report contains Turkish section prose.
- BUY/SELL/HOLD enum values remain stable unless a display-only translation layer is explicitly added.
</acceptance_criteria>
</task>

<task id="3">
<type>test</type>
<title>Cover language selection</title>
<files>tests/unit/test_localization.py</files>
<action>
Add tests for default English, explicit Turkish metadata, and report rendering behavior.
</action>
<acceptance_criteria>
- Tests prove English default is unchanged.
- Tests prove Turkish mode is explicit and does not affect structured signal values.
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `fish -c "uv run pytest tests/unit/test_localization.py"`
2. Generate one English report and one Turkish report from fixture data.
</verification>

<success_criteria>
- Apex supports Turkish output as an optional final enhancement without destabilizing the English-first MVP.
</success_criteria>
