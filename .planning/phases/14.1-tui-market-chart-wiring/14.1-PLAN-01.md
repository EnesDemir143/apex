---
phase: 14.1
plan: 01
type: execute
wave: 1
depends_on: [14]
files_modified:
  - src/apex/services/market_snapshot.py
  - src/apex/tui/app.py
  - src/apex/tui/commands.py
  - src/apex/tui/widgets.py
  - tests/unit/test_market_snapshot.py
  - tests/unit/test_tui_app.py
  - tests/unit/test_tui_commands.py
autonomous: true
branch: feat/tui-market-chart-wiring
requirements: [TUI-12]
---

<objective>
Replace the TUI market placeholder with real/local OHLCV and indicator summaries, and add a `/chart` terminal screen with ticker selection.
</objective>

<branching>
Before implementation, create/switch to feature branch: `feat/tui-market-chart-wiring`. Keep commits for this inserted phase on that branch.
</branching>

<tasks>
<task id="1">
<type>execute</type>
<title>Add local market snapshot service</title>
<files>src/apex/services/market_snapshot.py, tests/unit/test_market_snapshot.py</files>
<action>
Implement a no-LLM market snapshot helper that fetches recent OHLCV for a whitelisted ticker, falls back deterministically when live data is unavailable, and computes latest OHLCV plus indicator summaries using existing indicator utilities.
</action>
<acceptance_criteria>
- Service returns ticker, recent bars, latest open/high/low/close/volume, indicator summary, and source label.
- Service is deterministic under fallback and unit-testable without credentials/network.
- Invalid/non-whitelisted ticker is rejected consistently with local analysis.
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Wire MarketPanel to selected ticker snapshot</title>
<files>src/apex/tui/widgets.py, src/apex/tui/app.py</files>
<action>
Replace the static Open/High/Low/Close placeholder with latest snapshot data. Refresh when selected ticker changes and keep a graceful fallback/loading/error state.
</action>
<acceptance_criteria>
- Changing ticker updates visible OHLCV/volume/indicator summary.
- Panel does not require LLM calls or a running FastAPI server.
- Failures show safe fallback/error copy without crashing the TUI.
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Add `/chart` command and terminal chart screen</title>
<files>src/apex/tui/commands.py, src/apex/tui/app.py, src/apex/tui/widgets.py</files>
<action>
Add `/chart [TICKER]` that opens a dedicated chart screen. If no ticker is provided, use the selected ticker. Support ticker picker flow consistent with `/select` so users can type `/chart n` or choose from dropdown-style suggestions.
</action>
<acceptance_criteria>
- `/chart` opens a terminal-native chart screen for the current ticker.
- `/chart NVDA` opens the chart for NVDA and syncs selected ticker when appropriate.
- Chart screen shows price sparkline, volume summary, latest OHLCV, and indicator summary.
- `/chart` does not rerun agent analysis or call LLMs.
</acceptance_criteria>
</task>

<task id="4">
<type>execute</type>
<title>Add tests for market panel and chart routing</title>
<files>tests/unit/test_market_snapshot.py, tests/unit/test_tui_app.py, tests/unit/test_tui_commands.py</files>
<action>
Cover snapshot fallback, ticker validation, `/chart` dispatch, chart screen mount, and selected ticker updates.
</action>
<acceptance_criteria>
- Unit tests cover no-network fallback path.
- TUI tests cover `/chart` screen availability without manual terminal interaction.
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `uv run pytest tests/unit/test_market_snapshot.py tests/unit/test_tui_app.py tests/unit/test_tui_commands.py -q`
2. `uv run ruff check src/apex/services/market_snapshot.py src/apex/tui/app.py src/apex/tui/commands.py src/apex/tui/widgets.py tests/unit/test_market_snapshot.py tests/unit/test_tui_app.py tests/unit/test_tui_commands.py`
3. `uv run mypy src/apex/services/market_snapshot.py src/apex/tui/app.py src/apex/tui/commands.py src/apex/tui/widgets.py`
</verification>

<success_criteria>
- The TUI cockpit can show selected ticker OHLCV/indicator state and a terminal-native chart before report/history persistence begins.
</success_criteria>
