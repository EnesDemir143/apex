---
phase: 13
plan: 01
type: execute
wave: 1
depends_on: [12]
files_modified:
  - pyproject.toml
  - src/apex/services/local_analysis.py
  - src/apex/cli/__init__.py
  - src/apex/cli/main.py
  - tests/unit/test_local_analysis.py
  - tests/unit/test_cli.py
autonomous: true
branch: feat/local-analysis-cli-foundation
requirements: [TUI-02, TUI-03]
---

<objective>
Create a server-independent local analysis service and app entrypoint foundation for the future interactive cockpit.
</objective>

<branching>
Before implementation, create/switch to feature branch: `feat/local-analysis-cli-foundation`. Use standard feature branch naming (`feat/<slug>`), not `bet5/` or `codex/`. Keep commits for this phase on that branch.
</branching>

<tasks>

<task id="1">
<type>execute</type>
<title>Add local analysis service seam</title>
<files>src/apex/services/local_analysis.py</files>
<read_first>
- src/apex/agents/workflow.py
- src/apex/agents/state.py
- src/apex/ingestion/market_data_fetcher.py
</read_first>
<action>
Implement `run_local_analysis(ticker: str, mode: str = "full", analysis_date: date | str | None = None, extra_instructions: str | None = None, agent_instructions: dict[str, str] | None = None)` that canonicalizes ticker, normalizes/validates as-of date, prepares AgentState, invokes the existing workflow, and returns a structured result without persisting to Postgres.
Use existing fallback paths where credentials/data are unavailable.
</action>
<acceptance_criteria>
- Function can be imported and called without FastAPI app startup.
- Does not require Postgres/Redis to be running.
- Preserves AgentState keys expected by existing agents.
- Defaults analysis_date to current local/latest available date and rejects or warns on future dates.
- Accepts optional global and per-agent instructions for future TUI use, even if initial agents only surface them in prompt/context minimally.
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Add Apex app entrypoint shell</title>
<files>src/apex/cli/__init__.py, src/apex/cli/main.py, pyproject.toml</files>
<action>
Update `[project.scripts]` so `apex` points to the app entrypoint. Add a default command that can later open the TUI cockpit. Keep `apex analyze TICKER` as a secondary/dev-friendly command that calls local analysis and renders final signal/confidence/errors with Rich.
</action>
<acceptance_criteria>
- `uv run apex --help` shows app/TUI-oriented commands.
- `uv run apex analyze AAPL` remains available as secondary classic mode.
- CLI exits non-zero only on unrecoverable setup errors.
</acceptance_criteria>
</task>

<task id="3">
<type>test</type>
<title>Cover local service and CLI with fakes</title>
<files>tests/unit/test_local_analysis.py, tests/unit/test_cli.py</files>
<action>
Add tests that monkeypatch workflow/LLM calls as needed so CLI contract is tested without network/API keys.
</action>
<acceptance_criteria>
- Tests prove CLI command calls local analysis seam.
- Tests do not require real OpenAI/Alpaca credentials.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `fish -c "uv run apex --help"`
2. `fish -c "uv run apex analyze AAPL --help"`
3. `fish -c "uv run pytest tests/unit/test_local_analysis.py tests/unit/test_cli.py"`
4. `fish -c "uv run ruff check src/apex/cli src/apex/services/local_analysis.py tests/unit/test_cli.py tests/unit/test_local_analysis.py"`
</verification>

<success_criteria>
- Apex has a local-first app entrypoint and analysis runtime that Phase 14 can turn into the main interactive cockpit.
</success_criteria>
