---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apex/services/rag_pipeline.py
  - src/apex/services/sanitizer.py
  - src/apex/services/dead_letter_queue.py
  - src/apex/services/distributed_lock.py
autonomous: true
requirements: [COOL-01, COOL-02, COOL-03]
---

<objective>
Implement RAG pipeline stub with Nomic embeddings and pgvector search, input canonicalization/sanitization, dead letter queue, and distributed lock via Redis.
</objective>

<must_haves>
- truths:
  - "RAG pipeline returns top-K docs using Nomic embeddings + cosine search"
  - "Input sanitization strips dangerous content"
  - "Dead letter queue captures failed messages for retry"
  - "Distributed lock prevents concurrent analysis conflicts"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create RAG pipeline and sanitizer</title>
<files>src/apex/services/rag_pipeline.py, src/apex/services/sanitizer.py</files>
<read_first>
- src/apex/infrastructure_layer/models/embedding.py
- src/apex/infrastructure_layer/database.py
</read_first>
<action>
Create rag_pipeline.py: RAGPipeline class with embed_text() using Nomic Embed Text V2, search_similar(query, k=5) using pgvector cosine distance.
Create sanitizer.py: sanitize_text() stripping HTML tags, SQL injection patterns, prompt injection patterns. canonicalize_ticker() for uppercase + strip.
</action>
<acceptance_criteria>
- rag_pipeline.py contains `class RAGPipeline`
- rag_pipeline.py contains cosine similarity or `<=>` operator
- sanitizer.py contains `def sanitize_text`
- sanitizer.py contains `def canonicalize_ticker`
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create DLQ and distributed lock</title>
<files>src/apex/services/dead_letter_queue.py, src/apex/services/distributed_lock.py</files>
<read_first>
- src/apex/infrastructure_layer/redis_client.py
</read_first>
<action>
Create dead_letter_queue.py: DeadLetterQueue with push/pop/retry_all, stored in Redis list "apex:dlq".
Create distributed_lock.py: wrapper around redis.lock() with configurable timeout.
</action>
<acceptance_criteria>
- dead_letter_queue.py contains `class DeadLetterQueue`
- dead_letter_queue.py contains `apex:dlq`
- distributed_lock.py contains `redis` and `lock`
</acceptance_criteria>
</task>

</tasks>

<verification>
1. RAG pipeline embeds and retrieves documents
2. Sanitizer strips injection attempts
3. DLQ stores and retrieves failed messages
</verification>

<success_criteria>
- RAG pipeline functional with pgvector
- Input sanitization hardened
- DLQ and distributed lock operational
</success_criteria>
