---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: [13]
files_modified:
  - src/apex/reports/__init__.py
  - src/apex/reports/markdown.py
  - src/apex/reports/writer.py
  - src/apex/services/history_store.py
  - src/apex/cli/main.py
  - tests/unit/test_reports.py
  - tests/unit/test_history_store.py
autonomous: true
branch: feat/reports-history-replay
requirements: [TUI-06, TUI-07]
---

<objective>
Persist local analysis outputs as markdown reports and queryable local history.
</objective>

<branching>
Before implementation, create/switch to feature branch: `feat/reports-history-replay`. Use standard feature branch naming (`feat/<slug>`), not `bet5/` or `codex/`. Keep commits for this phase on that branch.
</branching>

<tasks>
<task id="1">
<type>execute</type>
<title>Generate markdown analysis reports with sectioned layout</title>
<files>src/apex/reports/markdown.py, src/apex/reports/writer.py</files>
<action>
Implement report generation from local analysis result/state. Include summary, technical/fundamental/risk/portfolio sections, disagreements, cost/tokens, errors, and disclaimer. Save both consolidated `complete_report.md` and section folders inspired by TradingAgents but Apex-specific.
</action>
<acceptance_criteria>
- `complete_report.md` includes final signal, confidence, agent outputs, caveats, and cost/tokens.
- Report directory contains Apex-specific sections: `1_technical/technical.md`, `2_fundamental/fundamental.md`, `3_risk/risk.md`, `4_portfolio/decision.md`, plus `state.json` and `metadata.json`.
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Add JSONL history store</title>
<files>src/apex/services/history_store.py</files>
<action>
Implement append/list/latest operations for local analysis metadata. Keep API independent from DB repositories.
</action>
<acceptance_criteria>
- History can list latest runs and filter by ticker.
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Expose report/history/replay CLI commands</title>
<files>src/apex/cli/main.py</files>
<action>
Add `--save-report` to analyze plus `apex history`, `apex report TICKER --latest`, and `apex replay PATH` commands.
</action>
<acceptance_criteria>
- Saved report path is printed after analysis.
- Replay renders saved state/report without rerunning LLM.
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `fish -c "uv run pytest tests/unit/test_reports.py tests/unit/test_history_store.py"`
2. `fish -c "uv run apex analyze AAPL --save-report"`
3. Inspect generated `reports/AAPL/.../complete_report.md`.
</verification>

<success_criteria>
- The project has durable local outputs that make the TUI useful and portfolio-friendly.
</success_criteria>
