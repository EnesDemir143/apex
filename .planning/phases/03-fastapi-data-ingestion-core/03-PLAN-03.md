---
phase: 3
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - tests/integration/test_health.py
  - tests/integration/test_data_client.py
  - tests/integration/conftest.py
  - tests/conftest.py
autonomous: true
requirements: [TEST-01]
---

<objective>
Create integration tests for health endpoints (postgres + redis E2E) and data client imports using testcontainers.
</objective>

<must_haves>
- truths:
  - "Integration tests validate health endpoint with real postgres and redis"
  - "Test fixtures use testcontainers for isolated environments"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create test fixtures with testcontainers</title>
<files>tests/conftest.py, tests/integration/conftest.py</files>
<read_first>
- tests/__init__.py
- tests/integration/
</read_first>
<action>
Create tests/conftest.py with shared fixtures.
Create tests/integration/conftest.py with:
- postgres_url fixture using PostgresContainer("pgvector/pgvector:pg17")
- redis_url fixture using RedisContainer("redis:8-alpine")
- app_client fixture using httpx.AsyncClient with FastAPI TestClient
</action>
<acceptance_criteria>
- tests/integration/conftest.py contains `PostgresContainer`
- tests/integration/conftest.py contains `RedisContainer`
- tests/integration/conftest.py contains `@pytest.fixture`
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create health endpoint integration tests</title>
<files>tests/integration/test_health.py</files>
<read_first>
- src/apex/api/routes/health.py
- tests/integration/conftest.py
</read_first>
<action>
Create tests/integration/test_health.py:
- test_health_endpoint_returns_200: GET /health returns status, postgres, redis fields
- test_ready_endpoint: GET /ready returns 200 when deps are up
- test_health_includes_version: response contains version field
</action>
<acceptance_criteria>
- test_health.py contains `async def test_health_endpoint`
- test_health.py contains `/health`
- test_health.py contains `assert` statements checking response status
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `uv run pytest tests/integration/ -v --tb=short` runs tests (may skip if no Docker)
2. Test files import correctly: `uv run python -c "import tests.integration.test_health"`
</verification>

<success_criteria>
- Integration test files created and properly structured
- Tests validate health endpoint with real infrastructure
</success_criteria>
