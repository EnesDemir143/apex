---
phase: 3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apex/api/app.py
  - src/apex/api/routes/__init__.py
  - src/apex/api/routes/health.py
  - src/apex/api/middleware.py
  - src/apex/api/dependencies.py
autonomous: true
requirements: [API-01, API-02]
---

<objective>
Create FastAPI application with lifespan management, correlation ID middleware, and /health + /ready endpoints with postgres and redis status checks.
</objective>

<must_haves>
- truths:
  - "FastAPI app with async lifespan context manager"
  - "GET /health returns 200 with postgres + redis status"
  - "GET /ready returns 200 only when all deps connected"
  - "Correlation ID middleware injects X-Correlation-ID"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create FastAPI app with lifespan</title>
<files>src/apex/api/app.py, src/apex/api/dependencies.py</files>
<read_first>
- src/apex/core/config.py
- src/apex/core/logging.py
</read_first>
<action>
Create src/apex/api/app.py:
- Import Settings, setup_logging
- asynccontextmanager lifespan: setup logging on startup, log shutdown on exit
- FastAPI(title="Apex", version=settings.app_version, lifespan=lifespan)
- Include health router with prefix=""
- Add CorrelationIDMiddleware
- Add CORSMiddleware for dev

Create src/apex/api/dependencies.py:
- get_db_session dependency (placeholder — actual session in Phase 5)
- get_redis dependency (placeholder)
</action>
<acceptance_criteria>
- src/apex/api/app.py contains `FastAPI(title="Apex"`
- src/apex/api/app.py contains `asynccontextmanager`
- src/apex/api/app.py contains `lifespan`
- src/apex/api/dependencies.py exists
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create correlation ID middleware</title>
<files>src/apex/api/middleware.py</files>
<read_first>
- src/apex/core/logging.py
</read_first>
<action>
Create src/apex/api/middleware.py with CorrelationIDMiddleware:
- Extract X-Correlation-ID from request headers or generate uuid4().hex[:16]
- Call set_correlation_id() from core.logging
- Add X-Correlation-ID to response headers
- Use BaseHTTPMiddleware from starlette
</action>
<acceptance_criteria>
- middleware.py contains `class CorrelationIDMiddleware`
- middleware.py contains `X-Correlation-ID`
- middleware.py contains `set_correlation_id`
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Create health and ready endpoints</title>
<files>src/apex/api/routes/__init__.py, src/apex/api/routes/health.py</files>
<read_first>
- src/apex/core/config.py
</read_first>
<action>
Create src/apex/api/routes/health.py:
- GET /health: async check postgres (SELECT 1) and redis (PING), return JSON status
- GET /ready: return 200 if all healthy, 503 if any dependency down
- Use asyncpg for postgres check, redis-py for redis check
- Return: {status, postgres, redis, version, environment}

Create src/apex/api/routes/__init__.py with docstring.
</action>
<acceptance_criteria>
- health.py contains `@router.get("/health")`
- health.py contains `@router.get("/ready")`
- health.py contains `SELECT 1` or `select 1`
- health.py contains `PING` or `ping`
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `uv run python -c "from apex.api.app import app; print(app.title)"` outputs "Apex"
2. `uv run uvicorn apex.api.app:app --port 8001` starts without errors (Ctrl+C to stop)
3. `curl http://localhost:8001/health` returns JSON with status field
</verification>

<success_criteria>
- FastAPI app starts and serves requests
- /health and /ready endpoints functional
- Correlation IDs propagated in headers and logs
</success_criteria>
