---
phase: 3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apex/ingestion/base_market_data_client.py
  - src/apex/ingestion/alpaca_client.py
  - src/apex/ingestion/yfinance_client.py
  - src/apex/ingestion/market_data_fetcher.py
  - src/apex/domain/models/ohlcv.py
  - src/apex/domain/models/__init__.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
---

<objective>
Build the market data ingestion pipeline: abstract MarketDataClient interface, Alpaca primary provider, yfinance fallback with DEGRADED flag, and Pydantic OHLCV validation model.
</objective>

<must_haves>
- truths:
  - "MarketDataClient ABC defines fetch_bars interface"
  - "AlpacaClient uses alpaca-py StockHistoricalDataClient"
  - "YFinanceClient activates as fallback with DEGRADED flag"
  - "OHLCVBar Pydantic model validates data quality"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create OHLCV Pydantic model</title>
<files>src/apex/domain/models/__init__.py, src/apex/domain/models/ohlcv.py</files>
<read_first>
- src/apex/domain/__init__.py
</read_first>
<action>
Create src/apex/domain/models/ohlcv.py:
- OHLCVBar(BaseModel): ticker (str), timestamp (datetime), open (Decimal), high (Decimal), low (Decimal), close (Decimal), volume (int), source (str = "alpaca")
- Validators: high >= low, close > 0, volume >= 0
- OHLCVResponse(BaseModel): bars (list[OHLCVBar]), ticker (str), source (str), degraded (bool = False)

Create src/apex/domain/models/__init__.py with exports.
</action>
<acceptance_criteria>
- ohlcv.py contains `class OHLCVBar(BaseModel)`
- ohlcv.py contains `high >= low` or `@field_validator`
- ohlcv.py contains `class OHLCVResponse`
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create MarketDataClient ABC</title>
<files>src/apex/ingestion/base_market_data_client.py</files>
<read_first>
- src/apex/domain/models/ohlcv.py
</read_first>
<action>
Create src/apex/ingestion/base_market_data_client.py:
- Abstract class MarketDataClient(ABC)
- Abstract method: `async def fetch_bars(self, ticker: str, start: date, end: date, timeframe: str = "1D") -> list[OHLCVBar]`
- Abstract property: `name: str` (provider name)
</action>
<acceptance_criteria>
- base_market_data_client.py contains `class MarketDataClient(ABC)`
- base_market_data_client.py contains `async def fetch_bars`
- base_market_data_client.py contains `@abstractmethod`
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Create Alpaca client implementation</title>
<files>src/apex/ingestion/alpaca_client.py</files>
<read_first>
- src/apex/ingestion/base_market_data_client.py
- src/apex/core/config.py
</read_first>
<action>
Create src/apex/ingestion/alpaca_client.py:
- AlpacaClient(MarketDataClient) using alpaca-py StockHistoricalDataClient
- Init: create StockHistoricalDataClient with api_key/secret_key from Settings
- fetch_bars: create StockBarsRequest, call get_stock_bars, convert BarSet to list[OHLCVBar]
- Handle alpaca API errors with structured logging
</action>
<acceptance_criteria>
- alpaca_client.py contains `class AlpacaClient(MarketDataClient)`
- alpaca_client.py contains `StockHistoricalDataClient`
- alpaca_client.py contains `StockBarsRequest`
</acceptance_criteria>
</task>

<task id="4">
<type>execute</type>
<title>Create yfinance fallback client</title>
<files>src/apex/ingestion/yfinance_client.py</files>
<read_first>
- src/apex/ingestion/base_market_data_client.py
</read_first>
<action>
Create src/apex/ingestion/yfinance_client.py:
- YFinanceClient(MarketDataClient) using yfinance.download()
- Convert DataFrame rows to OHLCVBar with source="yfinance"
- Log warning when fallback activates
</action>
<acceptance_criteria>
- yfinance_client.py contains `class YFinanceClient(MarketDataClient)`
- yfinance_client.py contains `yf.download` or `yfinance`
- yfinance_client.py contains `source="yfinance"`
</acceptance_criteria>
</task>

<task id="5">
<type>execute</type>
<title>Create MarketDataFetcher with failover</title>
<files>src/apex/ingestion/market_data_fetcher.py</files>
<read_first>
- src/apex/ingestion/alpaca_client.py
- src/apex/ingestion/yfinance_client.py
</read_first>
<action>
Create src/apex/ingestion/market_data_fetcher.py:
- MarketDataFetcher with primary (Alpaca) and fallback (yfinance) clients
- fetch_bars: try primary, on exception switch to fallback and set degraded=True
- Return OHLCVResponse with degraded flag
- Log provider switch with structured logging
</action>
<acceptance_criteria>
- market_data_fetcher.py contains `class MarketDataFetcher`
- market_data_fetcher.py contains `degraded`
- market_data_fetcher.py contains try/except pattern for failover
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `uv run python -c "from apex.ingestion.base_market_data_client import MarketDataClient"` imports OK
2. `uv run python -c "from apex.domain.models.ohlcv import OHLCVBar"` imports OK
3. OHLCV model rejects invalid data (high < low)
</verification>

<success_criteria>
- Data pipeline architecture established with ABC interface
- Alpaca and yfinance clients implemented
- Failover with DEGRADED flag working
- OHLCV validation model functional
</success_criteria>
