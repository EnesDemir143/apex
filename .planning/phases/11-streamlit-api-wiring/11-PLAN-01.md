---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apex/frontend/api_client.py
  - src/apex/frontend/pages/1_Dashboard.py
  - src/apex/frontend/pages/2_Signals.py
  - src/apex/frontend/pages/6_Observability.py
autonomous: true
requirements: [FE-02]
---

<objective>
Wire Streamlit Dashboard, Signals, and Observability pages to real FastAPI endpoints.
Replace mock_data imports with api_client calls. Keep mock as fallback on API error.
</objective>


<must_haves>
- truths:
  - "Dashboard shows real BUY/SELL/HOLD signals from POST /api/v1/analyze/{ticker}"
  - "Agent consensus and latest analysis come from real API response"
  - "Observability page shows real /health status"
  - "Mock data used as fallback when API is unreachable"
  - "No backend changes — frontend wiring only"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Extend api_client.py with batch signal fetch</title>
<files>src/apex/frontend/api_client.py</files>
<read_first>
- src/apex/core/constants.py
- src/apex/frontend/mock_data.py
</read_first>
<action>
Add fetch_all_signals(tickers) — calls fetch_analysis() for each ticker, maps response to signal row dict matching TOP_SIGNALS shape: {symbol, name, signal, confidence, risk, agreement, last_analysis}.
Add fetch_observability() — calls fetch_health(), returns dict matching OBSERVABILITY shape.
</action>
<acceptance_criteria>
- api_client.py contains `fetch_all_signals`
- api_client.py contains `fetch_observability`
- Both functions fall back gracefully (return empty list / default dict) on error
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Wire 1_Dashboard.py to real API</title>
<files>src/apex/frontend/pages/1_Dashboard.py</files>
<action>
Replace TOP_SIGNALS, HERO_METRICS, get_consensus(), get_latest_analysis() mock imports with api_client calls.
- TOP_SIGNALS → fetch_all_signals(TICKERS_WHITELIST)
- get_consensus(sym) → from fetch_analysis(sym) summary.agent_outputs
- get_latest_analysis(sym) → from fetch_analysis(sym)
- HERO_METRICS → derived from signal list (count, avg confidence, strongest signal)
- OBSERVABILITY / OBS_SPARKLINES → fetch_observability()
Keep BACKTEST_SUMMARY, BACKTEST_SPARKLINES, MARKET_REGIME from mock_data (no API).
Wrap API calls in st.spinner(). On None response fall back to mock_data equivalent.
</action>
<acceptance_criteria>
- 1_Dashboard.py does NOT import TOP_SIGNALS, HERO_METRICS, get_consensus, get_latest_analysis, OBSERVABILITY, OBS_SPARKLINES from mock_data
- 1_Dashboard.py imports from api_client
- BACKTEST_* and MARKET_REGIME still imported from mock_data
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Wire 2_Signals.py to real API</title>
<files>src/apex/frontend/pages/2_Signals.py</files>
<action>
Replace ALL_SIGNALS, get_consensus(), get_latest_analysis() with api_client calls.
- ALL_SIGNALS → fetch_all_signals(TICKERS_WHITELIST)
- get_consensus(sym) / get_latest_analysis(sym) → fetch_analysis(sym)
</action>
<acceptance_criteria>
- 2_Signals.py does NOT import ALL_SIGNALS, get_consensus, get_latest_analysis from mock_data
</acceptance_criteria>
</task>

<task id="4">
<type>execute</type>
<title>Wire 6_Observability.py to real API</title>
<files>src/apex/frontend/pages/6_Observability.py</files>
<action>
Replace OBSERVABILITY, OBS_SPARKLINES with fetch_observability() call.
LLM cost breakdown: derive from fetch_all_signals results (sum cost_usd per ticker).
</action>
<acceptance_criteria>
- 6_Observability.py does NOT import OBSERVABILITY, OBS_SPARKLINES from mock_data
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `uv run streamlit run src/apex/frontend/app.py` — Dashboard loads with real signals when API is up
2. `make check` green (ruff + mypy + pytest)
3. When API is down, pages show warning banner but don't crash
</verification>

<success_criteria>
- Dashboard, Signals, Observability pages show real data from FastAPI
- Backtest and Replay pages still use mock (no API exists)
- Graceful fallback on API unavailability
</success_criteria>
