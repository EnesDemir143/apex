---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - plans/BET5_POSTPROD_PLAN.md
autonomous: true
branch: feat/tui-pivot-product-cleanup
requirements: [TUI-01, DOC-01]
---

<objective>
Make the repo story and GSD planning surfaces TUI-first without deleting existing web/API work.
</objective>

<branching>
Before implementation, create/switch to feature branch: `feat/tui-pivot-product-cleanup`. Use standard feature branch naming (`feat/<slug>`), not `bet5/` or `codex/`. Keep commits for this phase on that branch.
</branching>

<must_haves>
- truths:
  - "Primary product direction is local-first CLI/TUI."
  - "Streamlit/FastAPI/Postgres remain preserved as optional/legacy extensions."
  - "Old web production frontend rewrite is not an active Bet 5 phase."
  - "README explains the pivot clearly and professionally."
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Update README product story to TUI-first</title>
<files>README.md</files>
<action>
Rewrite the opening sections so Apex is introduced as a local-first terminal cockpit for multi-agent market research. Add a short Project Direction / Pivot Note explaining the original web/API/dashboard path and why TUI became the primary path.
Move FastAPI/Streamlit/Postgres/K8s setup to optional/advanced sections.
</action>
<acceptance_criteria>
- First screen of README says local-first terminal/TUI cockpit.
- Optional web/API stack is still documented but no longer presented as required quickstart.
- No active web production frontend rewrite promise remains.
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Synchronize .planning project surfaces with Bet 5 pivot</title>
<files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
<action>
Update project definition, requirements, roadmap, and state to include Phase 12+ TUI pivot phases. Mark milestone as Bet 5 / Local-First TUI Pivot active. Preserve prior v1 completion history.
</action>
<acceptance_criteria>
- ROADMAP includes phases 12-17 in priority order.
- REQUIREMENTS includes TUI/DOC/local-first requirement IDs.
- STATE current phase is 12 and not complete.
</acceptance_criteria>
</task>

<task id="3">
<type>verify</type>
<title>Check planning consistency</title>
<files>.planning/ROADMAP.md, .planning/STATE.md, plans/BET5_POSTPROD_PLAN.md</files>
<action>
Check that the Bet 5 summary plan and .planning roadmap agree on phase order and no removed web rewrite items appear as active work.
</action>
<acceptance_criteria>
- Search for removed web-frontend/ad-monetization terms in `plans/BET5_POSTPROD_PLAN.md` and `.planning/ROADMAP.md`; any remaining mention must be historical or explicitly out-of-scope.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `fish -c "grep -n 'Phase 12' .planning/ROADMAP.md; grep -n 'TUI-01' .planning/REQUIREMENTS.md"`
2. Manual review: README top section is TUI-first.
3. Manual review: old web stack is preserved, not deleted.
</verification>

<success_criteria>
- Planning and README tell one coherent story: Apex Terminal is the main path; web stack is optional legacy/extension.
</success_criteria>
