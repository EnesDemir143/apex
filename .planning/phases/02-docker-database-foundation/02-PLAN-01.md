---
phase: 2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.dev.yml
  - docker-compose.prod.yml
  - docker/postgres/init.sql
  - docker/postgres/postgresql.conf
  - docker/redis/redis.conf
  - docker/observability/loki-config.yml
  - docker/observability/promtail-config.yml
  - docker/observability/grafana-datasources.yml
  - Dockerfile
  - .dockerignore
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05]
---

<objective>
Create Docker Compose dev/prod stacks with PostgreSQL 17 + pgvector, Redis 8, Grafana/Loki/Promtail observability, plus multi-stage Dockerfile for the Apex app.
</objective>

<must_haves>
- truths:
  - "docker compose -f docker-compose.dev.yml up starts all services without errors"
  - "PostgreSQL accepts connections with pgvector extension loaded"
  - "Redis 8 responds to PING — using redis:8-alpine NOT redis-stack"
  - "Multi-stage Dockerfile builds with non-root user"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create PostgreSQL init script and config</title>
<files>docker/postgres/init.sql, docker/postgres/postgresql.conf</files>
<read_first>
- docker/postgres/
</read_first>
<action>
Create docker/postgres/init.sql enabling pgvector extension.
Create docker/postgres/postgresql.conf with dev-tuned settings: listen_addresses='*', shared_buffers=256MB, max_connections=100, effective_cache_size=768MB, timezone=UTC.
</action>
<acceptance_criteria>
- docker/postgres/init.sql contains `CREATE EXTENSION IF NOT EXISTS vector`
- docker/postgres/postgresql.conf contains `shared_buffers = 256MB`
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create Redis 8 config</title>
<files>docker/redis/redis.conf</files>
<read_first>
- docker/redis/
</read_first>
<action>
Create docker/redis/redis.conf: bind 0.0.0.0, port 6379, appendonly yes, appendfsync everysec, maxmemory 256mb, maxmemory-policy allkeys-lru, protected-mode no (dev only).
</action>
<acceptance_criteria>
- docker/redis/redis.conf contains `appendonly yes`
- docker/redis/redis.conf contains `maxmemory-policy allkeys-lru`
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Create observability configs</title>
<files>docker/observability/loki-config.yml, docker/observability/promtail-config.yml, docker/observability/grafana-datasources.yml</files>
<read_first>
- docker/observability/
</read_first>
<action>
Create Loki config (auth disabled, TSDB+filesystem, port 3100), Promtail config (Docker container log scraping), Grafana datasource provisioning (Loki as default datasource).
</action>
<acceptance_criteria>
- loki-config.yml contains `auth_enabled: false`
- promtail-config.yml contains `docker`
- grafana-datasources.yml contains `type: loki`
</acceptance_criteria>
</task>

<task id="4">
<type>execute</type>
<title>Create docker-compose.dev.yml</title>
<files>docker-compose.dev.yml</files>
<read_first>
- docker/postgres/init.sql
- docker/redis/redis.conf
- docker/observability/loki-config.yml
</read_first>
<action>
Create docker-compose.dev.yml with services:
- postgres (pgvector/pgvector:pg17, port 5432, healthcheck pg_isready)
- redis (redis:8-alpine, port 6379, healthcheck redis-cli ping)
- loki (grafana/loki:3.4.0, port 3100)
- promtail (grafana/promtail:3.4.0)
- grafana (grafana/grafana:11.6.0, port 3000)
- redisinsight (redis/redisinsight:2.68, port 5540)
Named volumes, apex-net network.
</action>
<acceptance_criteria>
- docker-compose.dev.yml contains `pgvector/pgvector:pg17`
- docker-compose.dev.yml contains `redis:8-alpine`
- docker-compose.dev.yml contains `redisinsight`
- `docker compose -f docker-compose.dev.yml config` exits 0
</acceptance_criteria>
</task>

<task id="5">
<type>execute</type>
<title>Create docker-compose.prod.yml and Dockerfile</title>
<files>docker-compose.prod.yml, Dockerfile, .dockerignore</files>
<read_first>
- docker-compose.dev.yml
- pyproject.toml
</read_first>
<action>
Create docker-compose.prod.yml (no redisinsight, restart:unless-stopped, resource limits).
Create multi-stage Dockerfile: builder with uv sync → runtime with python:3.13-slim, non-root user apex (UID 1000), HEALTHCHECK.
Create .dockerignore excluding .git, .planning, .venv, __pycache__, tests, notebooks.
</action>
<acceptance_criteria>
- Dockerfile contains `FROM python:3.13-slim`
- Dockerfile contains `USER apex`
- Dockerfile contains `HEALTHCHECK`
- .dockerignore contains `.venv`
- docker-compose.prod.yml does NOT contain `redisinsight`
</acceptance_criteria>
</task>

<task id="6">
<type>execute</type>
<title>Initialize Alembic with async template</title>
<files>alembic.ini, migrations/env.py, migrations/script.py.mako</files>
<read_first>
- migrations/
- src/apex/core/config.py
</read_first>
<action>
Run `uv run alembic init -t async migrations` (if not already initialized).
Update migrations/env.py to import Settings from apex.core.config and use database_url.
Update alembic.ini sqlalchemy.url to empty (overridden by env.py).
</action>
<acceptance_criteria>
- alembic.ini exists and contains `[alembic]`
- migrations/env.py contains `from apex.core.config`
- `uv run alembic heads` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `docker compose -f docker-compose.dev.yml config` validates
2. `docker compose -f docker-compose.dev.yml up -d` starts all services
3. PostgreSQL: `docker exec` psql query confirms pgvector extension
4. Redis: `docker exec` redis-cli PING returns PONG
5. `docker build -t apex:dev .` builds successfully
6. `uv run alembic heads` shows no errors
</verification>

<success_criteria>
- docker compose up starts all services without errors
- PostgreSQL accepts connections with pgvector
- Redis 8 responds to PING
- Dockerfile builds with non-root user
- Alembic initialized and functional
</success_criteria>
