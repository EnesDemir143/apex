---
phase: 19
plan: 01
type: execute
wave: 1
depends_on: [14, 15, 17]
files_modified:
  - pyproject.toml
  - src/apex/agents/state.py
  - src/apex/agents/workflow.py
  - src/apex/agents/quant.py
  - src/apex/ml/__init__.py
  - src/apex/ml/features.py
  - src/apex/ml/device.py
  - src/apex/ml/model_registry.py
  - src/apex/tui/app.py
  - src/apex/tui/widgets.py
  - tests/unit/test_quant_agent.py
  - tests/unit/test_ml_device.py
autonomous: false
branch: feat/optional-quant-ml-agent
requirements: [ML-01, ML-02, ML-03]
---

<objective>
Add an optional Quant ML Agent with CPU/MPS/CUDA device selection and structured output that Portfolio Manager can consume.
</objective>

<branching>
Before implementation, create/switch to feature branch: `feat/optional-quant-ml-agent`. Use standard feature branch naming (`feat/<slug>`), not `bet5/` or `codex/`. Keep commits for this phase on that branch.
</branching>

<pre_execution_questions>
Ask user before implementation:
1. Start with dependency-light baseline model or add a dedicated ML dependency?
2. Device fallback behavior: hard fail when selected device unavailable, or warn and fall back to CPU?
3. Should Quant Agent be enabled by default or opt-in only?
</pre_execution_questions>

<tasks>
<task id="1">
<type>execute</type>
<title>Add ML feature and device utilities</title>
<files>src/apex/ml/features.py, src/apex/ml/device.py, src/apex/ml/__init__.py</files>
<action>
Implement OHLCV feature extraction and device resolution for auto/cpu/mps/cuda. Keep CPU as guaranteed path.
</action>
<acceptance_criteria>
- Feature builder returns deterministic feature rows from OHLCV.
- Device resolver reports selected device and availability reason.
- CPU path always works in tests.
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Add optional Quant Agent node</title>
<files>src/apex/agents/quant.py, src/apex/agents/state.py, src/apex/agents/workflow.py</files>
<action>
Add Quant Agent output to AgentState and wire it optionally into workflow. The initial model may be a baseline inference implementation if full training is deferred.
</action>
<acceptance_criteria>
- Quant Agent returns signal, confidence, reasoning/top features, model_version, and device.
- Existing 4-agent workflow still works when Quant Agent disabled.
- Portfolio Manager can include Quant output when present.
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Expose Quant/device controls in TUI</title>
<files>src/apex/tui/app.py, src/apex/tui/widgets.py</files>
<action>
Add optional Quant Agent enabled/disabled control, model selector placeholder, and ML device selector (auto/cpu/mps/cuda) to setup panel.
</action>
<acceptance_criteria>
- TUI setup shows Quant controls only after this feature exists.
- Selected device is included in report metadata/status.
</acceptance_criteria>
</task>

<task id="4">
<type>test</type>
<title>Test quant and device behavior</title>
<files>tests/unit/test_quant_agent.py, tests/unit/test_ml_device.py</files>
<action>
Add tests for CPU device resolution, unavailable accelerator behavior, feature extraction, and disabled Quant workflow compatibility.
</action>
<acceptance_criteria>
- Tests pass on machines without CUDA/MPS.
- Accelerator tests are skipped or mocked when hardware is unavailable.
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `fish -c "uv run pytest tests/unit/test_quant_agent.py tests/unit/test_ml_device.py"`
2. Run a local analysis with Quant disabled and verify no behavior regression.
3. Run a local analysis with Quant enabled on CPU and verify report metadata includes model/device.
</verification>

<success_criteria>
- Apex gains an optional ML/Quant signal and explicit CPU/MPS/CUDA device choice without destabilizing the TUI MVP.
</success_criteria>
