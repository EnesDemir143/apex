---
phase: 4
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/apex/ingestion/market_data_fetcher.py
  - src/apex/ingestion/market_calendar.py
  - scripts/seed_data.py
  - tests/unit/test_data_pipeline.py
  - tests/cassettes/
autonomous: true
requirements: [DATA-06, DATA-07, DB-03, TEST-02]
---

<objective>
Complete the data pipeline: idempotent upserts, market calendar filtering, stock splits handling, seed data script, and VCR.py test cassettes.
</objective>

<must_haves>
- truths:
  - "market_data_fetcher ingests tickers with upsert (no duplicates)"
  - "Market calendar filters to NYSE trading days"
  - "Seed data for AAPL, TSLA, MSFT, NVDA, GOOGL"
  - "VCR.py cassettes for offline testing"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Add upsert logic and market calendar</title>
<files>src/apex/ingestion/market_data_fetcher.py, src/apex/ingestion/market_calendar.py</files>
<read_first>
- src/apex/ingestion/market_data_fetcher.py
- src/apex/infrastructure_layer/models/stock_price.py
</read_first>
<action>
Update market_data_fetcher.py with upsert_bars() using PostgreSQL ON CONFLICT DO UPDATE.
Create market_calendar.py using pandas_market_calendars for NYSE trading day filtering.
Add stock splits handling via adj_close field from data providers.
</action>
<acceptance_criteria>
- market_data_fetcher.py contains `on_conflict_do_update` or `ON CONFLICT`
- market_calendar.py contains `pandas_market_calendars` or `mcal`
- market_calendar.py contains `NYSE`
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create seed data script</title>
<files>scripts/seed_data.py</files>
<read_first>
- src/apex/infrastructure_layer/models/stock.py
</read_first>
<action>
Create scripts/seed_data.py that inserts 5 stocks: AAPL (Apple, Technology, NASDAQ), TSLA (Tesla, Consumer Cyclical, NASDAQ), MSFT (Microsoft, Technology, NASDAQ), NVDA (NVIDIA, Technology, NASDAQ), GOOGL (Alphabet, Technology, NASDAQ). Use upsert to be idempotent.
</action>
<acceptance_criteria>
- seed_data.py contains `AAPL` and `TSLA` and `MSFT` and `NVDA` and `GOOGL`
- seed_data.py is runnable with `uv run python scripts/seed_data.py`
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Create VCR.py tests for data pipeline</title>
<files>tests/unit/test_data_pipeline.py</files>
<read_first>
- src/apex/ingestion/alpaca_client.py
</read_first>
<action>
Create tests with VCR.py cassettes for Alpaca API calls. Test idempotent upsert (run twice, verify no duplicates). Test market calendar filtering.
</action>
<acceptance_criteria>
- test_data_pipeline.py contains `@vcr.use_cassette` or `vcr`
- test_data_pipeline.py contains test for idempotent ingestion
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Seed data script runs without errors
2. Duplicate ingestion produces no duplicate rows
3. VCR cassettes recorded for Alpaca
</verification>

<success_criteria>
- Data pipeline fully functional with idempotent upserts
- Market calendar integration working
- Seed data for 5 tickers
- VCR.py tests passing
</success_criteria>
