---
phase: 5
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apex/services/llm_client.py
  - src/apex/services/cost_guard.py
  - src/apex/infrastructure_layer/database.py
  - src/apex/infrastructure_layer/redis_client.py
  - src/apex/services/cache_service.py
  - src/apex/services/stock_repository.py
  - src/apex/services/analysis_repository.py
autonomous: true
requirements: [CORE-04, CORE-05, DB-04, DB-05, CACHE-01]
---

<objective>
Create LLM client abstraction (OpenAI + Fake), cost guard with daily budget limits, async DB session factory, repository pattern CRUD, and Redis cache service.
</objective>

<must_haves>
- truths:
  - "LLMClient ABC with OpenAI and Fake implementations"
  - "CostGuard blocks when daily budget exceeded"
  - "Async SQLAlchemy sessions via get_db_session"
  - "Redis cache with TTL and key prefix"
  - "Repository pattern for Stock and Analysis"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create LLM client and cost guard</title>
<files>src/apex/services/llm_client.py, src/apex/services/cost_guard.py</files>
<read_first>
- src/apex/core/config.py
- src/apex/core/exceptions.py
</read_first>
<action>
Create llm_client.py: LLMClient ABC with `async def generate()`, OpenAIClient using langchain-openai ChatOpenAI, FakeLLMClient with deterministic responses.
Create cost_guard.py: BudgetLimiter tracking daily token usage, raises LLMBudgetExceededError when exceeded, stores in Redis with daily key.
</action>
<acceptance_criteria>
- llm_client.py contains `class LLMClient(ABC)`
- llm_client.py contains `class OpenAIClient`
- llm_client.py contains `class FakeLLMClient`
- cost_guard.py contains `class BudgetLimiter`
- cost_guard.py contains `LLMBudgetExceededError`
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create database and Redis infrastructure</title>
<files>src/apex/infrastructure_layer/database.py, src/apex/infrastructure_layer/redis_client.py, src/apex/services/cache_service.py</files>
<read_first>
- src/apex/core/config.py
</read_first>
<action>
Create database.py: async engine, session factory, get_db_session dependency.
Create redis_client.py: async Redis connection pool, get_redis dependency.
Create cache_service.py: CacheService with get/set/invalidate, "apex:" key prefix, TTL support.
</action>
<acceptance_criteria>
- database.py contains `create_async_engine`
- database.py contains `async_sessionmaker`
- redis_client.py contains `redis.asyncio`
- cache_service.py contains `class CacheService`
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<title>Create repositories</title>
<files>src/apex/services/stock_repository.py, src/apex/services/analysis_repository.py</files>
<read_first>
- src/apex/infrastructure_layer/models/__init__.py
- src/apex/infrastructure_layer/database.py
</read_first>
<action>
Create StockRepository with get_by_ticker, get_all, create.
Create AnalysisRepository with create, get_by_stock, get_latest.
Both accept AsyncSession in constructor.
</action>
<acceptance_criteria>
- stock_repository.py contains `class StockRepository`
- analysis_repository.py contains `class AnalysisRepository`
- Both contain `AsyncSession` in __init__
</acceptance_criteria>
</task>

</tasks>

<verification>
1. All service modules import correctly
2. FakeLLMClient returns deterministic responses
3. CostGuard raises on budget exceeded
</verification>

<success_criteria>
- LLM client abstraction with OpenAI and Fake implementations
- Cost guard operational
- Database and cache services functional
- Repository pattern established
</success_criteria>
