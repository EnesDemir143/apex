---
phase: 6
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/apex/agents/hooks.py
  - src/apex/agents/tool_schemas.py
autonomous: true
requirements: [AGENT-08, SEC-01, SEC-02, SEC-03]
---

<objective>
Implement 3-layer security: pre-analysis hooks (whitelist, injection scan, budget), tool isolation via Pydantic schemas, and post-analysis hooks (output validation, confidence threshold).
</objective>

<must_haves>
- truths:
  - "Pre-hook blocks non-whitelisted tickers and prompt injections"
  - "Post-hook validates output schema and checks confidence threshold"
  - "TradeDecisionInput Pydantic schema for tool isolation"
  - "Usage tracker records token counts and estimated costs"
</must_haves>

<tasks>

<task id="1">
<type>execute</type>
<title>Create pre and post analysis hooks</title>
<files>src/apex/agents/hooks.py</files>
<read_first>
- src/apex/core/constants.py
- src/apex/services/cost_guard.py
- src/apex/agents/state.py
</read_first>
<action>
Create hooks.py:
- pre_analysis_hook(state): check ticker in TICKERS_WHITELIST, scan for prompt injection patterns, verify budget via CostGuard
- post_analysis_hook(state): validate portfolio_decision schema, check confidence >= MIN_CONFIDENCE, enforce instruction hierarchy
- Both return modified state or raise with structured error
</action>
<acceptance_criteria>
- hooks.py contains `def pre_analysis_hook`
- hooks.py contains `def post_analysis_hook`
- hooks.py contains `TICKERS_WHITELIST`
- hooks.py contains confidence validation
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<title>Create tool isolation schemas</title>
<files>src/apex/agents/tool_schemas.py</files>
<read_first>
- src/apex/domain/value_objects.py
</read_first>
<action>
Create tool_schemas.py with Pydantic models constraining agent tool outputs:
TradeDecisionInput(BaseModel): ticker (str), signal (Signal), confidence (float 0-1), reasoning (str), risk_score (float 0-1).
</action>
<acceptance_criteria>
- tool_schemas.py contains `class TradeDecisionInput(BaseModel)`
- tool_schemas.py contains `Signal` import
- tool_schemas.py contains `Field(ge=0.0, le=1.0)`
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Pre-hook blocks unknown ticker
2. Post-hook rejects invalid confidence
3. TradeDecisionInput validates properly
</verification>

<success_criteria>
- 3-layer security hooks functional
- Tool isolation via Pydantic schemas
</success_criteria>
