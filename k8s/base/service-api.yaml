apiVersion: v1
kind: Service
metadata:
  name: apex-api
  namespace: apex
  labels:
    app.kubernetes.io/name: apex-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: apex-api
  ports:
    - name: http
      port: 8000
      targetPort: http
